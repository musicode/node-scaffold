
const SECOND = 1000
const MINUTE = 60 * SECOND
const HOUR = 60 * MINUTE
const DAY = 24 * HOUR

module.exports = {
  SECOND,
  MINUTE,
  HOUR,
  DAY,

  /**
   * 格式化日期
   *
   * @param {*} date
   * @param {*} format
   */
  format(date, format) {

  }
}
